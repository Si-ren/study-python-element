复习:
	pandas中是字符串的操作
	和内容的python字符串操作一致
	
	pd.str.split
	pd.str.strip().str.replece()

本节课程:
	pandas可视化-->底层使用的是matplotlib

	实例演示:
	
# 绘制柱状图
result_q1['好评率'].plot(kind='bar',
       color = 'b',
       width = 0.8,
       alpha = 0.4,
       rot = 45,
       grid = True,
       ylim = [0.98,1],
       figsize = (12,4),
       title = '不同导演电影的好评率')

	